# Assurance / Proof-Obligation Governance Enforcement Plan

Status: **A_DESIGN correction candidate — implementation forbidden until fresh exact-head 0C/0M review and `DESIGN_ACCEPTED`**

## Goal

Make the assurance/proof-obligation policy fail-closed in ABCP before any new external code-bearing build. Exactly three capsules remain: A designs/freezes functional + assurance authority, B implements it, C performs branch acceptance/final review and owns the governed publication/merge sequence.

The controlling A model is `docs/plans/assurance-proof-obligation-governance-enforcement-assurance.md`. B mutation is forbidden until that exact model + this plan pass fresh independent 0 Critical / 0 Major A review.

## Task 1 — V4 contracts, drained activation, durable backend

Frozen obligations: `AG-PO-001..007`, `AG-PO-020..024`, `AG-PO-031`, `AG-PO-034`, `AG-PO-038`, `AG-PO-042`, `AG-PO-046..050`, `AG-PO-060`, `AG-PO-064`.

Implement the exhaustive strict-canonical field/digest/bound/predicate descriptor catalog and generated positive/per-constraint rejection vectors; literal rejection uses the frozen primitive-valid `CONSTANT_CHANGED`/operator rule, and extraction fails on any bare array, unresolved digest, unbounded field, filesystem/DDL identity mismatch or unenumerated predicate. The registry must explicitly resolve both controller stage-tip fields, selected evidence, and the PR/MERGE-constrained attempt indexes with zero unresolved targets. Serialize all 70 stable scenarios directly with one of the 14 closed `FailureDimension` literals. Implement the shared `PredecessorAuthorityDirectoryV1` for the exact workflow row, every bound ledger/run, process-startup PR admission root and merge state root. `internal/ledger/**` is explicitly in B authority because shared writer fencing, byte-exact `EffectLedgerEventPreimageV1` reconstruction and typed V4 effect barriers must be enforced at the ledger mutation boundary without constructor randomness/current time. Retained event IDs remain byte-exact at their historical producer width: `NewEvent` uses 16 random bytes/32 lower-hex characters, while retained deterministic PR/merge/CI events use all 32 SHA-256 bytes/64 lower-hex characters. V4 alone deterministically uses the first 16 bytes of its frozen SHA-256 preimage as 32 lower-hex and never truncates, widens or reinterprets a historical ID. Every V1 CAS/ledger/provider writer takes the registered shared cutover lease. `FencedWorkflowAuthorityBackendV1` wraps the exact selected predecessor backend, so the exclusive fence quiesces even a non-PostgreSQL predecessor before loading stable canonical V1 bytes. The SERIALIZABLE successor transaction proves exact lease states `{ISSUED,CONSUMING,CONSUMED}` and all barriers drained, preserves exact V1 bytes, tombstones later wrapped V1 CAS/provider admission, and installs V2 at `v1.revision+1`; grandfather set is empty. Implement the exact five-file manifest and executable forced-RLS role/function/policy model: eleven tables, 35 policies and all ten literal function bodies plus their canonical request/result wires are frozen; schema/table owner, ordinary runtime, separately credentialed recovery verifier, domain-function owner and worker-function owner are distinct NOLOGIN/NOBYPASSRLS group roles; policies use validated `session_user`; and fresh bootstrap verifies every source/normalized-body digest, owner, grant, policy and FORCE flag. Cutover alone admits a 4,259,848-byte canonical request so three independently decoded 1 MiB fields plus at most 65,536 bytes of JSON overhead always fit; every other function retains the 1 MiB cap. No live V3 migration is permitted.

## Task 2 — A gate, capsule/grant/checkpoint authority, semantic corrections

Frozen obligations: `AG-PO-006`, `AG-PO-008..013`, `AG-PO-018..019`, `AG-PO-023..027`, `AG-PO-031`, `AG-PO-040`, `AG-PO-047..049`.

Implement `ContextCapsuleV4`, `PhaseAuthorityV4`, `PhaseCheckpointV2` and `StageGrantV2` as additive successors retaining every V3 capsule/source/outcome/parent field and every V1 grant/checkpoint blocker/invariant/operation/final-review floor. Use the frozen acyclic root bootstrap: A capsule without lineage, current checkpoint-core→A→B grant→full checkpoint sealing without lineage, then `AcceptedALineageV1`; every descendant carries it. Bind exact full parent checkpoint **and** grant. Preserve policy/model/registry/review plus independent proof-set, evidence-matrix, resource-profile-set and canonical-vector-catalog digests through A→B→C and failed-C→correction-grant→correction-B→replacement-C. Correction paths remain only `SemanticAuthorityRegistryV2 ∩ original B scope ∩ ControllerAffectedPathSetV1` from controller-recomputed accepted-B base→failed-candidate Git diff; reviewer bytes never supply paths.

## Task 3 — evidence, resource, artifact and secret execution

Frozen obligations: `AG-PO-013..016`, `AG-PO-020..025`, `AG-PO-027..036`, `AG-PO-039..041`, `AG-PO-050..052`, `AG-PO-061..063`.

Implement worker-scoped shared PostgreSQL admission and lifecycle transitions across all domains/controllers/workflows. Bootstrap installs exactly one immutable per-worker `WorkerObservationKeyRegistrationV1`, pinning a 32-raw-byte/64-lower-hex Ed25519 public key to worker capacity/provenance; every worker reservation and `RuntimeOwnerIdentityV1` pins that root, and ordinary runtime cannot read, mint or replace it. Every validator binds one flattened direct `MaterializedResourceProfileV1`; a J-05 root separately freezes its larger allocation envelope and explicit direct counters. Aggregate admission checks combined process+container+unique-daemon memory/PID/CPU/cache/log/image ceilings; child creation locks/freezes the immutable parent and checks `direct + Σchildren ≤ envelope` in every dimension, while siblings share the same remainder and add no root charge. The literal worker transition function locks worker+reservation and uses identity plus worker/reservation CAS to execute only `RESERVED→RUNNING→SETTLING→RELEASED`, with crash/ambiguity from any active state to fully charged `RECOVERY_HOLD` and proved `RECOVERY_HOLD→RELEASED`; runtime has no direct worker DML. Separate immutable blobs from stage occurrences. Implement exact publisher open/append/finalize/crash-abandon operations under stage→publisher lock order; every literal mutation compares locked domain/lineage/stage/subject before effects. Abandon requires the exact runtime PID/start/boot/cgroup/container owner and signed fresh process/cgroup/container absence observations with 64-raw-byte/128-lower-hex signatures from the registered worker observer, mechanically verified by a different-host process using the separate recovery-verifier DB role. That role can read only its domain's workflow/artifact/stage/publisher/occurrence/worker-reservation rows and only referenced non-tenant shared capacity/registration facts; it has no mutator EXECUTE or cross-domain authority. Only that verifier may insert a short-lived one-use `PublisherAbandonAuthorizationV1`; ordinary runtime cannot read/mint one, and SQL consumes it atomically with ACTIVE→ABANDONED, so a generic same-domain blob has no mutation path. Close returns bounded recovery work and fixes a cutoff only after zero exact-stage ACTIVE publishers. Inventory remains ≤16 Merkle-linked pages ×128 entries, with closure artifacts outside the occurrence set. Derive `StageDiffLineageV1` by stage and require exact subject/base/candidate equality through candidate artifact, inventory, scan authority/evidence and rejection vectors. `CODEX_BROKER_V1` uses only the frozen profile/probes; no credential-bearing stage passes without full exact-pair zero-match evidence.

## Task 4 — C, integration, PR/merge effects and ledger handoff

Frozen obligations: `AG-PO-010..023`, `AG-PO-025..029`, `AG-PO-031..037`, `AG-PO-043`, `AG-PO-045`, `AG-PO-054..059`.

C executes only C cells against unchanged candidate and fresh exact-head review. `internal/integrationgate/**` must durably stop at `INTEGRATION_ACCEPTED`. Pre-READY `EP005ReadyCompatibilitySourceV1` freezes the retained constructor inputs without any READY output; `PRPublicationAuthorityV1` binds that source, event/run/candidate and complete PR bytes. PR APPLIED alone derives readiness, after which `GitHubMergeAuthorityAdapterV1` uses only the frozen additive `assurance-pr-effect-v4`/`ready-for-merge-evidence-v1` branches to construct retained binding/content/policy and invoke unchanged `NewAuthority`. The legacy `integration-gate`/`serial-integration-gate-decision` branch and historical bytes remain unchanged. PR and merge requests bind one typed `ProviderAuthenticationContextV1` and exact non-durable mutation `ProviderCredentialUseV1` capability through typed identity/repository-scope reads, request, claim/admission, transport, result/reconciliation and APPLIED observation. GitHub App JWT identity is distinct from installation-token identity/scope, and reusable credential bytes are never retained. Under `AcquireRunTransition`, install the typed embedded `EffectLedgerBarrierV4`; ledger append/removal accepts only exact byte-bound `EffectLedgerTransitionAuthorityV1` or typed settlement/unclaimed proof. Provider admission revalidates the exact source event/authentication/credential under that lease. Implement one-winner claims and durable `CALL_POSSIBLE`; every response/result and its `EffectAttemptIndexV1` pointer commit atomically. Only an exact locked result-slot-absent competing CAS creates `EffectReconciliationAnchorV1`, then ordinal-1 read-only reconciliation. Fresh recovery follows `ControllerStateV2→index→result/anchor/reconciliation/settlement`. `EffectReauthorizationV1` permits `n+1` only after settled NOT_APPLIED plus exact resolution.

The append-only ledger remains run-state truth and PostgreSQL remains coordination authority. Exact PR APPLIED permits `MergeReadinessAuthorityV1` to create `ReadyForMergeEvidenceV1` and the sole byte-exact `INTEGRATION_ACCEPTED→READY_FOR_MERGE` event before PG `PR_PUBLISHED`; merge APPLIED creates only byte-exact `READY_FOR_MERGE→MERGED` before PG `MERGE_APPLIED`. `internal/githublifecycle/**` receives only the additive compatibility-source/PR parser and exact post-PR V4 READY expected-content/binding adapter branches needed under retained EP-005 head/baseline/tree semantics; legacy validation is not weakened. `internal/integrationgate/**` removes only its premature READY step. NOT_APPLIED resolves typed barrier proof; UNKNOWN retains it. Post-merge failure preserves factual MERGED bytes.

## Task 5 — assurance escapes and learning evidence

Frozen obligations: `AG-PO-013`, `AG-PO-026`, `AG-PO-040`, `AG-PO-048`.

Persist `AssuranceEscapeV1` and mark A-required for foreseeable Critical/Major model escapes. Learning records never mutate live authority. No score/threshold bypass exists.

## Task 6 — deterministic dogfood and post-merge acceptance

Frozen obligations: `AG-PO-023..064` plus cumulative verification of every earlier obligation.

Execute J-01..J-05 exactly. J-05 uses the frozen four-file toy repository, exact plan, baseline commit `eb301906130c0d7ac13b7d90942d469753ab7708`, tree `6b50353385765d0809844bc6ef2415ce112f7e2b`, concrete floor/budget/broker/tool profiles, a full root allocation envelope, a smaller parent direct profile and one positive child direct/allocation fixture with all stated hashes recomputed. Parent direct plus child equals the envelope in the constrained process dimensions; the child consumes the envelope's container/daemon capacity, adds zero worker-root charge, and any second positive sibling rejects. Delegation permits child A design/review only; child A creates its repository-bound model before B/C. PR/merge/deployment/nested dogfood remain forbidden.

Negative acceptance proves: unregistered/non-drained V3 blocks V4; tombstone blocks later V1 CAS; direct and SECURITY DEFINER cross-domain DB access fails; all ten function body/wire drifts fail fresh bootstrap; cutover accepts the exact three-maximum-field bound but rejects >65,536-byte overhead or any decoded field >1 MiB; blob reuse cannot omit an occurrence; cross-stage publisher substitution fails every literal mutation; active/late publisher blocks close until signed fresh exact-owner cross-host absence and verifier-only one-use capability prove recovery-abandon; runtime-selected/unregistered/replaced key, wrong registration root, wrong Ed25519 wire length, generic runtime JSON/blob and runtime capability-row mint/read fail; the verifier can read every required same-domain row and no other-domain tenant row or unreferenced shared worker fact, and cannot call/update/delete/DDL; every legal worker lifecycle edge succeeds while skipped/stale/wrong-identity transitions and a non-charging `RECOVERY_HOLD` fail; append cannot race close; 2,048 occurrences inventory completely; empty/unrelated/reversed/stale diff pair fails; split/weaker profile, direct+child overflow, nested double-charge and sibling overflow fail; reviewer paths cannot mint authority; two correction grants only; PR/merge has one winner; wrong credential commitment, app/installation scope, principal, repository, body, PR, ref or route cannot settle APPLIED; pre-call loss makes no call; result/index transaction and anchor race has one winner and survives fresh restart; competing FAILED/CANCELLED or non-exact V4 event bytes cannot pass the typed barrier; retained historical 32- and 64-character event IDs both pass byte-exact compatibility, malformed retained widths fail, and the frozen V4 truncation vector passes while a 64-character V4 effect ID fails; integration cannot emit READY, PR authority cannot depend on READY, legacy EP-005 bytes still validate, and non-PR-derived V4 READY fails; relative/absolute path and DB identity mismatches reject; all 70 scenario dimensions serialize directly to the closed enum; exhaustive digest extraction resolves all five controller-state names and prints zero unresolved; every literal has a primitive-valid `CONSTANT_CHANGED` rejection and every untyped/unbounded/unpredicated descriptor fails.

## Frozen non-goals

- no fourth capsule;
- no live V3 migration/grandfather continuation during V4 activation;
- no universal evidence class for every task beyond the frozen matrix;
- no reviewer-created mutation authority;
- no retroactive reinterpretation of historical V1/V2/V3 bytes;
- no replacement of deterministic acceptance with model review;
- no `internal/githublifecycle/**` change beyond additive pre-READY `EP005ReadyCompatibilitySourceV1`/`PRPublicationAuthorityV1` and the exact post-PR V4 `ReadyForMergeEvidenceV1` expected-content/READY-binding/`GitHubMergeAuthorityAdapterV1` branches; retained legacy constructors, canonical bytes and `NewAuthority` are unchanged, and no `internal/integrationgate/**` change is permitted beyond stopping clean integration at `INTEGRATION_ACCEPTED`;
- no external-build readiness claim until controller enforcement + dogfood + post-merge acceptance are complete.
